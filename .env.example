# Local-only ports. PostgreSQL, Redis and worker have no published ports.
FRONTEND_PORT=5173
API_PORT=5080
# Generate a local password with scripts/init-local.mjs. Never commit .env.
POSTGRES_PASSWORD=
