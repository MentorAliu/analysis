<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <EmbeddedResource Include="Scoring/Manifests/*.json" />
    <!-- Exact calculator source is hashed at runtime; no source files are published separately. -->
    <EmbeddedResource Include="Scoring/*.cs" />
    <EmbeddedResource Include="Observation.cs;Catalog.cs" />
  </ItemGroup>
</Project>
