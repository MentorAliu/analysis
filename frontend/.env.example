# All VITE_ inputs are public browser configuration. Never put secrets here.
VITE_APP_NAME=Research workspace
# Server-only Vite development proxy target; do not prefix with VITE_.
API_PROXY_TARGET=http://127.0.0.1:5080
